# engineering-runtime-capabilities

**Replica** of Runtime-embedded GitHub/Files examples, plus the broader
company library. Author here; Runtime Home `capabilities/` is a
non-authoritative cache. The governed push is `runtime github file put`.

Control Plane UI (portal Live + app) may catalog from a Runtime snapshot.
This checkout is the company library to author against.

See [`CLAUDE.md`](./CLAUDE.md) for authoring rules that still apply when
working against a runtime checkout (`RUNTIME_CAPABILITIES_DIR`).
