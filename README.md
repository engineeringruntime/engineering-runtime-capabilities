# engineering-runtime-capabilities

**Replica** of [`engineering-runtime/capabilities`](https://github.com/kishore-gutta/engineering-runtime/tree/main/capabilities).

Control Plane UI (portal Live + app) catalogs capabilities from the
**runtime** repo only. Prefer editing/adding capabilities there.

See [`CLAUDE.md`](./CLAUDE.md) for authoring rules that still apply when
working against a runtime checkout (`RUNTIME_CAPABILITIES_DIR`).
